((nil . ((eval . (progn
                         (add-to-list 'load-path
                           (expand-file-name
                             "dev"
                             (locate-dominating-file default-directory ".dir-locals.el")))
                         (require 'anita))))))
